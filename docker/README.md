# Docker

Containerization files for development and runtime.

- `Dockerfile` — base image (primarily used for testing and debugging)
- `docker-compose.yml` — runtime services
- `docker-compose.debug.yml` — debugging setup
