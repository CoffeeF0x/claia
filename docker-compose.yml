version: '3.9'

services:
  py:
    image: python:3.12-slim
    env_file:
      - .env
    volumes:
      - ./:/app

  dev:
    image: clai:dev
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./:/app
