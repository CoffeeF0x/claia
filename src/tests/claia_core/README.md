# claia_core tests

Tests for the pure-library layer (data models, results, plugin
metadata dataclasses, enums, ABCs).

Currently empty; populated as we add focused unit coverage for
`claia_core` modules.
