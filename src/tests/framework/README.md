# claia.framework tests

Tests for the framework layer:
- `test_process.py` — `Process` lifecycle and state transitions
- `test_queue.py` — `ProcessQueue` semantics and waits
- `test_registry.py` — `Registry.run()` solver/deployment dispatch
- `test_simple_agent.py` — built-in `SimpleAgent` plugin behaviour
