# Hooks

Plugin hook contracts for agents, models, tools, and related components.

Defines interfaces used by registries and the manager to discover and load extensions.

Files: `agent.py`, `architecture.py`, `definition.py`, `deployment.py`, `module.py`, `pattern.py`, `protocol.py`, `solver.py`.
