# Model Layer

Model architecture layer and adapters.

Subpackages:
- `api/` — hosted LLM APIs
- `base/` — shared base classes
- `dummy/` — test/dummy models
- `transformers/` — local transformer-based models

Supports per-extension `required_args` filtering via registries.
