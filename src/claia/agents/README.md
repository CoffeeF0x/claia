# Agents

Agent implementations that orchestrate model calls and tools.

- `simple.py` — minimal agent forwarding prompts and kwargs
- `bob.py` — example/advanced agent behavior

Notes:
- Agents use `lib.process`, `lib.queue`, and models via registries.
