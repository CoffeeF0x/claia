# Zammad Tool Module

Integration with Zammad (helpdesk) APIs.

- `api.py`, `constants.py`, `utils.py`, `zammad.py` — API client, helpers, and commands

Requires appropriate credentials via `required_args`-filtered kwargs.
