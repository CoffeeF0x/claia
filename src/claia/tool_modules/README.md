# Tool Modules

Concrete tool integrations providing callable commands.

- `sample.py` — example commands
- `zammad/` — Zammad service integration

Registries expose a commands catalog; protocols execute via that catalog.
